@if (DataLoader == null 
    || DataLoader.LoadingState == LoadingState.None 
    || DataLoader.LoadingState == LoadingState.Finished)
    return;

<MudText Typo="Typo.overline">@DataLoader.LoadingState.ToString() @Title</MudText>

@code {
    [Parameter]
    public DataLoader? DataLoader { get; set; }

    [Parameter]
    public string? Title { get; set; }
}
